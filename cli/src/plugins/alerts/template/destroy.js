/*
 * destroy.js
 *
 * Template destroy command.
 */

// Main packages
const logger = require('../../../logger')

module.exports = async () => {
  logger.info('')
  logger.info('You ran the destroy command for the template alerts plugin.')
}
