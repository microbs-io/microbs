/*
 * setup.js
 *
 * Template setup command.
 */

// Main packages
const logger = require('../../../logger')

module.exports = async () => {
  logger.info('')
  logger.info('You ran the setup command for the template alerts plugin.')
}
